class CartItem {
  constructor(quantity, servicePrice, serviceTitle, sum) {
    this.quantity = quantity;
    this.servicePrice = servicePrice;
    this.serviceTitle = serviceTitle;
    this.sum = sum;
  }
}

export default CartItem;
